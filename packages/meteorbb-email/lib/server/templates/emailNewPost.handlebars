<span class="heading">
<a href="{{profileUrl}}">{{postAuthorName}}</a>
发表了一个帖子:
{{#if url}}
  <a href="{{linkUrl}}" class="action-link">{{postTitle}}}</a>
{{else}}
  {{postTitle}}}
{{/if}}
</span><br><br>

{{#if htmlBody}}
  <div class="post-body">
  {{{htmlBody}}}
  </div>
  <br>
{{/if}}

<a href="{{postUrl}}">点击回复</a><br><br>
