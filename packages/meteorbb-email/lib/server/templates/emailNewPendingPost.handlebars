<span class="heading">
<a href="{{profileUrl}}">{{postAuthorName}}</a>
审核通过:
{{#if url}}
  <a href="{{linkUrl}}" class="action-link">{{postTitle}}}</a>
{{else}}
  {{postTitle}}}
{{/if}}
</span><br><br>

{{#if htmlBody}}
  <div class="post-body">
  {{{htmlBody}}}
  </div>
  <br>
{{/if}}

<a href="{{postUrl}}">访问</a><br><br>
